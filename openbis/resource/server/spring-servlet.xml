<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xsi:schemaLocation="
                http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/tx
                        http://www.springframework.org/schema/tx/spring-tx.xsd">

    <bean id="urlMapping" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <!-- Note that two mappings for the same servlet are necessary for some unknown
                 reason in order to run successfully in hosted mode, web mode, and productive mode. -->
            <props>
                <prop key="generic">generic-servlet</prop>
                <prop key="genericopenbis/generic">generic-servlet</prop> 
            </props>
        </property>
    </bean>

    <bean id="generic-servlet" class="ch.systemsx.cisd.openbis.generic.client.web.server.GenericClientServiceServlet" />

</beans>